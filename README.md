## Extract Stage

- Runs `python -m src.extract` to fetch from LINKS, normalize URIs (e.g., hy2 -> hysteria2), and save to protocol-specific files in output/.
- Config-driven via PROXIES for easy extension.
